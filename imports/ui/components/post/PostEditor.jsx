import React from "react";
import gql from "graphql-tag";
import { PropTypes } from "prop-types";

const PostEditor = ({ slug }) => <h1>PostEditor</h1>;

PostEditor.propTypes = {
  slug: PropTypes.string
};

export default PostEditor;
